{
  "name": "table-annotator",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Human-in-the-loop table annotator for building tables_extended ground-truth .test.json files: parses table PDFs with the LlamaParse worker cli2, then serves a web app to review each table beside its page screenshot and refine the transcription with one or more LLMs.",
  "bin": {
    "annotator": "./annotateTables.mjs"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.40.0",
    "@google/genai": "^1.42.0",
    "openai": "^4.95.0"
  }
}
